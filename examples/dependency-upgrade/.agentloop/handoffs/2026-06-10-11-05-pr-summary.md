# PR Summary

## Summary

- Upgraded the validation library for a parser bugfix.
- Updated `package.json` and `pnpm-lock.yaml`.
- Confirmed the existing validation checks still pass.

## Changed Files

- `package.json`
- `pnpm-lock.yaml`
- `tests/validation.test.ts`

## Verification

- `pnpm install --frozen-lockfile`: pass
- `pnpm test`: pass
- `pnpm typecheck`: pass
- `pnpm build`: pass

## Verification Not Performed

- Package advisory scan was not performed.
- Production smoke test was not performed.
- Manual changelog review was not recorded in this sample.

## Risks

- Lockfile changes need human review.
- Passing tests do not prove the upgraded package is safe.
- Reviewers should inspect transitive dependency churn.

## Rollback

- Revert `package.json` and `pnpm-lock.yaml`.

## Reviewer Checklist

- Confirm only the intended package changed.
- Confirm lockfile changes match the package-manager operation.
- Confirm no unexpected install scripts or native builds appeared.
- Confirm verification evidence is enough for the dependency risk.
