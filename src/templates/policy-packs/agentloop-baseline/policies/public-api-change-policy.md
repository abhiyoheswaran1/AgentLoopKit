# Public API Change Policy

Treat public API changes as compatibility-sensitive.

- Identify affected users, commands, config fields, and output shapes.
- Keep machine-readable output additive unless a breaking release is planned.
- Update docs and examples when behavior changes.
